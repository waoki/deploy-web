BUILDING
--------

To build test packages, install git-buildpackage, ensure your pbuilder is
working, then:

    BUILDER=pbuilder gbp buildpackage --git-dist=stretch --git-ignore-branch

For release:

    BUILDER=pbuilder gbp buildpackage  --git-dist=stretch --git-tag


